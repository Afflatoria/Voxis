# Minimal CosyVoice 3 inference dependencies for Windows.
# Do NOT use third_party/CosyVoice/requirements.txt on Windows — it pins Linux-only
# packages (grpcio source build, tensorrt, deepspeed) and old grpcio that fails to compile.
#
# Install:
#   python -m pip install -r requirements/cosyvoice-windows.txt
#
# PyTorch: use your existing CUDA install if present (2.3+). Uncomment below only if needed:
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.3.1
# torchaudio==2.3.1

conformer==0.3.2
diffusers==0.29.0
HyperPyYAML==1.2.2
ruamel.yaml>=0.17.0,<0.19.0
setuptools>=70.0.0,<81.0.0
hydra-core==1.3.2
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
modelscope>=1.20.0
numpy>=1.26.4,<2.0
omegaconf==2.3.0
onnx==1.16.0
onnxruntime==1.18.0
# openai-whisper installed separately by scripts/install_cosyvoice_windows.py (--no-deps)
protobuf==4.25
regex>=2023.0
transformers>=4.51.3,<4.58
x-transformers==2.11.24
wetext==0.0.4
wget==3.2
rich>=13.7.0
gdown>=5.1.0
pyarrow>=18.0.0
pyworld>=0.3.4
matplotlib>=3.7.0
networkx>=3.1
